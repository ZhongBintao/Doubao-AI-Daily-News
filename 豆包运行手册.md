# AI 每日早报 · 豆包自动化运行手册

> 本手册是豆包每日执行 AI 每日早报生成任务的操作指南。
> 项目根目录：`/home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin/`
> Python 解释器：`OpenMontage/.venv/bin/python`

---

## 快速运行（推荐 · 云电脑原生）

v2.0 起，项目提供单入口编排器 `ai_morning_brief.daily`，自动调度全流程 9 个阶段，
断点续跑，仅在 3 个需要 LLM 的阶段（写稿、语音克隆、封面）交接给 Agent。

### 1. 环境准备

```bash
cd /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin
git pull --ff-only || echo "继续使用本地版本"
bash scripts/bootstrap_env.sh || exit 1
source .local/env.sh
```

### 2. 运行编排器

```bash
OpenMontage/.venv/bin/python -m ai_morning_brief.daily --date $(date +%Y-%m-%d)
```

编排器自动执行，遇到 Agent 阶段打印交接指令并退出。完成交接后**重新运行同一命令**即可继续。

### 3. 三个 Agent 交接点

| 阶段 | 你需要做的事 |
|------|------------|
| **editorial** 写稿 | 读 `artifacts/editorial_task.json` → 写 `artifacts/editorial_plan.json` → 运行校验命令确保 pass |
| **voice_clone** 语音克隆 | 读 `artifacts/tts_manifest.json` → 逐段调用 `audio_to_audio_plus`（@音频1=example-audio.mp3）→ 保存到指定路径 |
| **cover** 封面 | 读 `release-kit/covers/cover_task.json` → 用 `image_edit` 生成 16:9/3:4/9:16 三张 → 保存到指定路径 |

### 4. 常用命令

```bash
# 查看进度
python -m ai_morning_brief.daily --date YYYY-MM-DD --status
# 强制重跑
python -m ai_morning_brief.daily --date YYYY-MM-DD --force
# 跳过封面和发布
python -m ai_morning_brief.daily --date YYYY-MM-DD --skip cover,release
# 从渲染阶段重跑
python -m ai_morning_brief.daily --date YYYY-MM-DD --from render
```

---

## 手动分步运行（调试用 · 8 步）

> 以下为原始手动流程，适用于编排器故障时的调试和补跑。日常运行请使用上方快速运行。

---

## 触发方式

- **定时触发**：每天早上 7:30 由豆包定时任务自动触发
- **手动触发**：用户说"生成今天的 AI 每日早报"或"补跑 YYYY-MM-DD 的早报"
- **日期规则**：默认取当天日期（Asia/Shanghai）。手动补跑时用户指定日期

---

## 执行流程（共 8 步）

默认交付三件套：视频、三种尺寸封面、发布文案整合包。三者都是默认交付物，不因用户没提而跳过。

### 第 0 步：环境自检（10 秒；重置后首次约 3-8 分钟）

云电脑运行一段时间后会重置，因此**每次运行都要自检**，不要假设环境还在：

```bash
cd /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin

# 拉取最新代码（目录已存在时）
git pull --ff-only || echo "拉取最新代码失败，继续使用本地版本"

# 幂等自检：venv / Node>=22 / ffmpeg+ffprobe / .env，缺失自动补装到 .local/
bash scripts/bootstrap_env.sh || exit 1

# 注入 PATH（ffmpeg 与 node 都在项目内，不注入子进程找不到）
source .local/env.sh
```

输出 `BOOTSTRAP_OK` 才可继续；若以 `[env][FATAL]` 终止，报告错误并停止，不要继续。

下文**每一条**命令都默认已执行过 `source .local/env.sh`。

---

### 第 1 步：冻结素材（1-2 分钟）

运行 pipeline prepare，从 AIHOT 抓取过去 24 小时精选资讯并选择 6-8 条：

```bash
OpenMontage/.venv/bin/python -m ai_morning_brief.pipeline prepare \
  --date YYYY-MM-DD \
  --env-file .env \
  --source-visual-mode off
```

**检查产出**：
- `outputs/YYYY-MM-DD/artifacts/source_snapshot.json` 存在
- `outputs/YYYY-MM-DD/artifacts/selection_report.json` 中 `selected_count >= 3`
- `outputs/YYYY-MM-DD/artifacts/editorial_input.json` 存在
- `outputs/YYYY-MM-DD/artifacts/writing_request.json` 存在
- `outputs/YYYY-MM-DD/artifacts/editorial_draft.json` 存在

如果选中资讯不足 3 条，记录"今日素材不足"并终止，不要用旧闻或虚构内容填充。

---

### 第 2 步：编辑写稿（5-10 分钟，豆包完成）

**这一步由豆包（你）直接完成，不需要运行外部命令。**

#### 2.1 读取输入

读取以下三个文件的**完整内容**：
1. `outputs/YYYY-MM-DD/artifacts/editorial_input.json` — 冻结的资讯数据（含每条的 title、summary、claims、来源链接）
2. `outputs/YYYY-MM-DD/artifacts/writing_request.json` — 写稿请求结构
3. `outputs/YYYY-MM-DD/artifacts/editorial_draft.json` — 确定性草稿（作为结构参考，不要直接照抄）

同时参考写稿规范：`skills/ai-brief-editorial-writer/references/editorial-contract.md`

#### 2.2 生成 editorial_plan.json

按照 v5 规范生成编辑计划，写入 `outputs/YYYY-MM-DD/artifacts/editorial_plan.json`。

**核心规范**：
- 计划版本 `5.0`，`writer.version = 4.1`
- 每条故事有：`subject`（具体主题）、`navigation_title`（导航标题）、`overview_text`（概览文案，必须引用 summary 中的具体事实）、`overview_claim_ids`、连续的 `presentation_order`
- 每个 narration beat 有稳定 `beat_id`，beat 内容必须完整解释事件，不设字数上限
- 卡片数量由有效 claim 决定，每张卡有 `id`、`subject`、`headline`、`body`、`claim_ids`
- 短条目（同维度、证据简短、2-4 条）可用 `story_kind: brief_group`；复杂条目必须 `single`
- 5-8 条短条目需平衡分组（3+2、3+3、4+3、4+4），不能有 1 条组
- **严禁添加来源没有的数字、因果、预测、建议、品牌名**
- 不把 score、URL、AIHOT 链接写进视频文案或卡片
- `overview_text` 不能只写品牌名或导航标签，必须陈述具体事件/结果/能力/限制

#### 2.3 质量校验

写完后运行项目内置校验：

```bash
OpenMontage/.venv/bin/python -c "
import json, sys
from pathlib import Path
from ai_morning_brief.editorial import build_editorial_quality_report, load_editorial_plan
from ai_morning_brief.models import SourceItem

date = 'YYYY-MM-DD'
run_dir = Path('outputs') / date
artifacts = run_dir / 'artifacts'

plan = load_editorial_plan(artifacts / 'editorial_plan.json')
editorial_input = json.loads((artifacts / 'editorial_input.json').read_text(encoding='utf-8'))
source_items = {item['item_id']: SourceItem.from_mapping(item) for item in editorial_input.get('items', [])}
report = build_editorial_quality_report(plan, editorial_input, source_items)
json.dump(report, (artifacts / 'editorial_quality_report.json').open('w', encoding='utf-8'), ensure_ascii=False, indent=2)
print('Status:', report.get('status'))
if report.get('errors'):
    for e in report['errors'][:10]:
        print(' -', e)
sys.exit(0 if report.get('status') == 'pass' else 1)
"
```

如果 `status != pass`：
- 阅读错误信息，针对性修改 `editorial_plan.json`
- 重新运行校验
- 最多重试 2 次
- 2 次仍失败，记录错误详情并终止当天运行

---

### 第 3 步：生成旁白脚本（30 秒）

运行 pipeline 的脚本生成（这会基于 editorial_plan 生成 narration_plan.json，包含所有 segment 的 spoken_text）：

```bash
OpenMontage/.venv/bin/python -c "
import json
from pathlib import Path
from ai_morning_brief.script import build_script_from_editorial_plan, validate_script
from ai_morning_brief.editorial import load_editorial_plan, finalize_editorial_plan
from ai_morning_brief.selection import SelectionPolicy
from ai_morning_brief.models import SourceItem
from ai_morning_brief.media import write_json

date = 'YYYY-MM-DD'
run_dir = Path('outputs') / date
artifacts = run_dir / 'artifacts'

editorial_input = json.loads((artifacts / 'editorial_input.json').read_text(encoding='utf-8'))
plan = load_editorial_plan(artifacts / 'editorial_plan.json')
plan, _ = finalize_editorial_plan(plan)
write_json(artifacts / 'editorial_plan_final.json', plan)

source_items = {item['item_id']: SourceItem.from_mapping(item) for item in editorial_input.get('items', [])}
selection_items = tuple(source_items[item_id] for item_id in editorial_input['selection']['item_ids'] if item_id in source_items)

class Sel:
    def __init__(self):
        self.items = selection_items
        self.mode = editorial_input['selection']['status']
        self.category_counts = editorial_input['selection'].get('category_counts', {})
        self.reason = editorial_input['selection'].get('reason', '')
        self.eligible_count = editorial_input['selection'].get('eligible_count', 0)
        self.selection_metadata = {}

script = build_script_from_editorial_plan(Sel(), run_date=__import__('datetime').date.fromisoformat(date), editorial_input=editorial_input, editorial_plan=plan)
errors = validate_script(script, source_items, editorial_input)
if errors:
    print('Script validation failed:', errors[:5])
    raise SystemExit(1)
write_json(artifacts / 'narration_plan.json', script)
print('narration_plan.json generated, segments:', len(script['segments']))
"
```

**检查**：`outputs/YYYY-MM-DD/artifacts/narration_plan.json` 存在，包含 intro、overview、若干 story、outro 等 segment。

---

### 第 4 步：豆包 TTS 语音合成（5-15 分钟，豆包完成）

> v2.0 起默认使用**语音克隆**模式（`audio_to_audio_plus` + `example-audio.mp3`）。
> 如需使用旧的描述式 TTS，在 prepare/finalize 时加 `--tts-mode text-to-audio`。

#### 4.1 生成待合成清单

```bash
OpenMontage/.venv/bin/python -m ai_morning_brief.doubao_tts_adapter prepare --date YYYY-MM-DD
```

这会输出每个 segment 的 id、文本长度和目标保存路径，并生成 `artifacts/tts_manifest.json`。

#### 4.2 逐个合成语音（豆包操作）

读取 `outputs/YYYY-MM-DD/artifacts/tts_manifest.json`，对其中每个 segment：

1. 调用豆包**语音克隆工具**（`audio_to_audio_plus`）：
   - `@音频1` = 项目根目录的 `example-audio.mp3`（音色参考，29.8秒）
   - prompt：`"用参考音频的音色、语速和朗读风格，清晰朗读以下文字，不增删字词，无背景音无杂音：{spoken_text}"`
2. 将生成的音频文件**保存到清单中指定的 output_path**（即 `assets/audio/narration-{segment_id}.wav`）
3. 所有 segment 都完成后，进入下一步

**注意**：
- 必须严格使用 spoken_text 原文，不增删字词
- 参考音频固定为 `example-audio.mp3`，全片音色一致
- 详细语音规范见 `skills/ai-brief-doubao-voice/SKILL.md`
- 如果某个 segment 合成失败，重试一次；仍失败则记录并终止

#### 4.3 验证并生成 manifest

```bash
OpenMontage/.venv/bin/python -m ai_morning_brief.doubao_tts_adapter status --date YYYY-MM-DD
OpenMontage/.venv/bin/python -m ai_morning_brief.doubao_tts_adapter finalize --date YYYY-MM-DD
```

`finalize` 会自动将所有音频转为 48kHz 单声道 WAV，读取时长，生成 `doubao_audio_manifest.json`。

如果 status 显示有 segment 未完成，回到 4.2 补合成。

---

### 第 5 步：视频渲染（5-15 分钟）

运行 pipeline run，复用已合成的豆包音频：

```bash
OpenMontage/.venv/bin/python -m ai_morning_brief.pipeline run \
  --date YYYY-MM-DD \
  --force \
  --reuse-source \
  --reuse-audio \
  --env-file .env \
  --speech-provider doubao-voice-clone
```

这一步自动完成：
- 加载 editorial_plan，生成脚本和发音 ledger
- 质量门禁校验
- 比例估算生成字幕（因为豆包 TTS 无字级时间戳）
- 人声拼接 + 背景音乐混音（opening/middle/ending 三段 BGM）
- HTML 模板物化
- OpenMontage HyperFrames 渲染（1920×1080 MP4）
- 视频质量门禁（时长、分辨率、音频流、文本溢出检测）

**检查产出**：
- `outputs/YYYY-MM-DD/run_report.json` 中 `status == "success"`
- `outputs/YYYY-MM-DD/artifacts/quality_report.json` 中 `status == "pass"`
- `outputs/YYYY-MM-DD/renders/ai-daily-news-YYYY-MM-DD.mp4` 存在且大小 > 0
- `outputs/YYYY-MM-DD/assets/subtitles/subtitles.srt` 存在

如果渲染失败，查看 `run_report.json` 中的 `failed_stage` 和 `error` 字段，记录错误详情。

---

### 第 6 步：封面生成（默认执行，5-10 分钟，豆包完成）

封面是默认交付物，必须生成 16:9 / 3:4 / 9:16 三种尺寸；不要询问用户是否需要。
详细规范见 `skills/ai-brief-cover-generator-doubao/SKILL.md`。

#### 6.1 生成封面请求

```bash
cd skills/ai-brief-cover-generator
OpenMontage/.venv/bin/python scripts/cover_workflow.py prepare \
  --editorial-input /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin/outputs/YYYY-MM-DD/artifacts/editorial_input.json \
  --item-id <封面故事 item_id> \
  --headline "<头条标题>" \
  --subheadline "<副标题>" \
  --visual-brief "<新闻视觉主题描述>" \
  --image-provider seedream \
  --force
```

输出：`outputs/YYYY-MM-DD/release-kit/covers/cover_request.json`

**检查**：确认 `image_provider` = `"seedream"`，每个 ratio 下有 `seedream_prompt` 字段（中文自然语言）。

#### 6.2 生成 16:9 锚点封面

使用豆包**图片编辑工具**（image_edit，不是 image_gen）：

- **参考图**（按顺序传入，最多 6 张）：
  1. `skills/ai-brief-cover-generator/assets/references/cover-positive-16x9.png`（理想效果参考，学习布局和视觉质量）
  2. `skills/ai-brief-cover-generator/assets/references/cover-style-system-16x9.png`（风格系统参考，学习视觉语言）
  3. 本期品牌 Logo 图（如有，在 `skills/ai-brief-cover-generator/assets/logos/` 下）
- **Prompt**：使用 `cover_request.json` 中 ratio=16:9 的 `seedream_prompt` 字段（**不要用原来的英文 `prompt` 字段**）
- **尺寸**：width=1920, height=1080

#### 6.3 文字准确性校验

生成后，用 VLM 识别图片中的文字，与 `cover_request.json` 的 `copy` 字段中四段预期文字对比：
1. `"AI每日早报"`
2. `"<date_label>"`（如 `"2026.09.05"`）
3. `"<headline>"`
4. `"<subheadline>"`

- 全部准确 → 通过，进入 6.4 记录
- 有错字/漏字/多余文字 → 重试 1 次（重新调用 image_edit，prompt 末尾追加"特别注意：四段文字必须准确无误"）
- 重试仍失败 → 接受当前结果，继续记录

#### 6.4 记录 16:9 封面

```bash
OpenMontage/.venv/bin/python scripts/cover_workflow.py record \
  --request /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin/outputs/YYYY-MM-DD/release-kit/covers/cover_request.json \
  --image 16:9=<16:9 图片路径> \
  --force
```

#### 6.5 生成 3:4 竖版封面

- **参考图**：刚生成的 `16x9.png` + `cover-style-system-16x9.png` + 品牌 Logo（如有）
- **Prompt**：`cover_request.json` 中 ratio=3:4 的 `seedream_prompt`（已包含"为竖版完全重新构图，不要裁切或拉伸"）
- **尺寸**：width=1080, height=1440
- 文字校验同 6.3，记录同 6.4（`--image 3:4=<路径>`）

#### 6.6 生成 9:16 竖版封面

同 6.5，尺寸 width=1080, height=1920，使用 ratio=9:16 的 `seedream_prompt`。

#### 6.7 完成确认

确认以下文件存在：
- `outputs/YYYY-MM-DD/release-kit/covers/16x9.png`
- `outputs/YYYY-MM-DD/release-kit/covers/3x4.png`
- `outputs/YYYY-MM-DD/release-kit/covers/9x16.png`
- `outputs/YYYY-MM-DD/release-kit/covers/cover_manifest.json`

---

### 第 7 步：发布文案与整合包（默认执行，2-3 分钟）

生成可直接发布的文案与整合包。规范见 `skills/ai-brief-release-kit/SKILL.md`。

#### 7.1 冻结发布文案

标题固定为 `AI每日早报YYYY-MM-DD`（使用冻结的期号日期，不要改写）。
描述必须基于 `editorial_input.json` 的真实内容：一到两个分句、用 `；` 连接，
不按平台拆分、不设字数上限、不添加来源没有的信息。

```bash
OpenMontage/.venv/bin/python skills/ai-brief-release-kit/scripts/release_workflow.py prepare \
  --editorial-input outputs/YYYY-MM-DD/artifacts/editorial_input.json \
  --description "<发布文案>" \
  --primary-item-id <头条 item_id> \
  --output-dir outputs/YYYY-MM-DD/release-kit \
  --force
```

产出 `outputs/YYYY-MM-DD/release-kit/release_plan.json`。

#### 7.2 组装整合包

```bash
OpenMontage/.venv/bin/python skills/ai-brief-release-kit/scripts/release_workflow.py finalize \
  --release-plan outputs/YYYY-MM-DD/release-kit/release_plan.json \
  --run-dir outputs/YYYY-MM-DD \
  --video outputs/YYYY-MM-DD/renders/ai-daily-news-YYYY-MM-DD.mp4 \
  --cover-manifest outputs/YYYY-MM-DD/release-kit/covers/cover_manifest.json \
  --output-dir outputs/YYYY-MM-DD/release-kit \
  --force
```

前置条件（缺一不可）：`run_report.json` 为 `success`、`artifacts/quality_report.json` 为 `pass`、
封面 `cover_manifest.json` 存在。封面缺失就先回到第 6 步补齐。

**检查**：`outputs/YYYY-MM-DD/release-kit/` 下同时含发布文案、打包好的视频与封面。

---

### 第 8 步：完成通知

汇总当天产出，告知用户。三件交付物缺一不可，若某项缺失必须说明原因，不能含糊带过：

```
✅ AI 每日早报 YYYY-MM-DD 生成完成

📹 视频：outputs/YYYY-MM-DD/renders/ai-daily-news-YYYY-MM-DD.mp4
   分辨率：1920×1080 | 时长：XX分XX秒 | 大小：XX MB

📝 资讯条数：X 条
🗣️ 语音：豆包语音克隆（audio_to_audio_plus + example-audio.mp3，比例估算字幕）
🎵 背景音乐：已混音

🖼️ 封面：
   16:9  outputs/YYYY-MM-DD/release-kit/covers/16x9.png
   3:4   outputs/YYYY-MM-DD/release-kit/covers/3x4.png
   9:16  outputs/YYYY-MM-DD/release-kit/covers/9x16.png

📦 发布文案包：outputs/YYYY-MM-DD/release-kit/
   标题：AI每日早报YYYY-MM-DD
   描述：<文案全文>

📂 完整产出目录：outputs/YYYY-MM-DD/
   ├── artifacts/（审计文件、质量报告、发音账本）
   ├── assets/audio/（人声分轨、最终混音）
   ├── assets/subtitles/subtitles.srt（字幕）
   ├── renders/（最终 MP4）
   └── release-kit/（发布文案 + 封面 + 整合包）

⚠️ 发布前请人工审听和审阅视频内容。
```

---

## 失败处理规则

| 失败阶段 | 处理方式 |
|---------|---------|
| 第1步 素材不足3条 | 终止，告知用户今日 AIHOT 素材不足 |
| 第2步 写稿校验2次仍失败 | 终止，保存错误的 editorial_plan 和质量报告供人工排查 |
| 第4步 某段语音合成失败 | 重试1次，仍失败则终止 |
| 第5步 视频渲染失败 | 查看 run_report.json 的 failed_stage，记录错误；音频已生成的情况下可用 `--reuse-audio` 重跑渲染 |
| 第6步 封面生成失败 | 不影响主视频，记录后跳过封面（此时第 7 步的前置条件不满足，一并跳过并在通知中说明） |
| 第7步 发布包组装失败 | 检查三个前置条件（run_report success / quality_report pass / cover manifest 存在），补齐后重跑 finalize |

任何阶段失败都要：
1. 记录失败阶段和错误信息
2. 不删除已生成的中间产物（可用于补跑）
3. 告知用户失败原因和已完成的部分

---

## 补跑说明

用户说"补跑 YYYY-MM-DD 的早报"时：
- 如果当天 `run_report.json` 已存在且 `status=success`，询问是否要 `--force` 重新生成
- 如果当天有部分产物（如音频已生成但渲染失败），从失败阶段继续，不要从头开始
- 音频已生成时用 `--reuse-audio` 跳过 TTS
- 素材已冻结时用 `--reuse-source` 跳过抓取

---

## 关键文件路径速查

| 文件 | 路径 |
|------|------|
| 编排器入口 | `python -m ai_morning_brief.daily` |
| 运行状态 | `outputs/YYYY-MM-DD/run_state.json` |
| 项目根目录 | `/home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin/` |
| Python 解释器 | `OpenMontage/.venv/bin/python` |
| 每日产出根目录 | `outputs/YYYY-MM-DD/` |
| 编辑计划 | `outputs/YYYY-MM-DD/artifacts/editorial_plan.json` |
| 写稿任务文件 | `outputs/YYYY-MM-DD/artifacts/editorial_task.json` |
| 旁白脚本 | `outputs/YYYY-MM-DD/artifacts/narration_plan.json` |
| TTS 合成清单 | `outputs/YYYY-MM-DD/artifacts/tts_manifest.json` |
| 人声目录 | `outputs/YYYY-MM-DD/assets/audio/` |
| 豆包音频清单 | `outputs/YYYY-MM-DD/artifacts/doubao_audio_manifest.json` |
| 最终视频 | `outputs/YYYY-MM-DD/renders/ai-daily-news-YYYY-MM-DD.mp4` |
| 字幕文件 | `outputs/YYYY-MM-DD/assets/subtitles/subtitles.srt` |
| 运行报告 | `outputs/YYYY-MM-DD/run_report.json` |
| 质量报告 | `outputs/YYYY-MM-DD/artifacts/quality_report.json` |
| 封面请求 | `outputs/YYYY-MM-DD/release-kit/covers/cover_request.json` |
| 封面成品 | `outputs/YYYY-MM-DD/release-kit/covers/{16x9,3x4,9x16}.png` |
| 封面清单 | `outputs/YYYY-MM-DD/release-kit/covers/cover_manifest.json` |
| 语音规范 | `skills/ai-brief-doubao-voice/SKILL.md` |
| 封面规范 | `skills/ai-brief-cover-generator-doubao/SKILL.md` |
| 发布计划 | `outputs/YYYY-MM-DD/release-kit/release_plan.json` |
| 发布整合包 | `outputs/YYYY-MM-DD/release-kit/` |
| 发布规范 | `skills/ai-brief-release-kit/SKILL.md` |
