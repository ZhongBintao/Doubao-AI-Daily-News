# 快速开始

## 一次性安装（约 2 分钟）

云电脑重置后 `.user_skills` 会被清空，这一步理论上要重做 ——
但只要跑过一次下面的「定时提示词」，豆包会自己重建，不必手动再来第二遍。

1. 克隆仓库：

```bash
git clone https://github.com/ZhongBintao/Doubao-AI-Daily-News.git \
  /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin
```

2. 把入口 skill 复制到豆包的 `.user_skills/`：

```bash
cp -r /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin/entry-skill/ai-daily-news-studio \
  ~/.super_doubao/super-doubao-runtime/workspace/.user_skills/
```

3. 完成。后续对话中豆包会自动发现这个 skill。

## 使用

在任意对话中说：

> 生成今天的 AI 每日早报

就这么简单。豆包会自动：

- 拉取最新代码
- 自检环境（Python venv、Node >= 22、ffmpeg/ffprobe），缺失就自动装到项目内 `.local/`
- 按流程生成视频、三张封面、发布文案包；稀疏日自动回退到最近 7 天未播资讯，
  如果完全没有未播资讯则生成 `no-news` 状态短报

## 定时任务提示词（每天 7:30 用这段）

这段提示词只负责「确保 skill 就位」，具体怎么做全部交给 skill：

> 生成今天的 AI 每日早报。
> 若 ~/.super_doubao/super-doubao-runtime/workspace/.user_skills/ai-daily-news-studio 不存在，
> 先克隆 https://github.com/ZhongBintao/Doubao-AI-Daily-News.git 到
> /home/user/.super_doubao/super-doubao-runtime/workspace/ai-daily-news-studio-codex-plugin，
> 并把其中的 entry-skill/ai-daily-news-studio 复制到 .user_skills/。
> 然后严格按 ai-daily-news-studio skill 的规定执行。
> 日期按 Asia/Shanghai 取当天；环境自检未通过就终止并报告；
> 24 小时精选为空时先审计查询 selected `window=7d`，排除前 7 个通过版本已播
> item ID，最多选择 3 条未播资讯；如果仍无未播资讯，生成 `no-news` 状态短报，
> 严禁用旧闻或虚构内容填充。

约定全部写在仓库的 skill 里，**改约定只需改仓库**，云电脑下次 `git pull` 自动生效，
不需要去改定时任务。

## 其他触发词

- "AI 早报"
- "今天的早报"
- "生成每日新闻视频"
- "补跑 2026-09-05 的早报"（指定日期补跑）

## 默认产出（三件套，不需要额外说明）

- 视频：`outputs/YYYY-MM-DD/renders/ai-daily-news-YYYY-MM-DD.mp4`（1920×1080）
- 字幕：`outputs/YYYY-MM-DD/assets/subtitles/subtitles.srt`
- 封面（16:9 / 3:4 / 9:16）：`outputs/YYYY-MM-DD/release-kit/covers/`
- 发布文案整合包：`outputs/YYYY-MM-DD/release-kit/`

## 固定配置（不需要每次说明）

- 语音：活泼快语速女声
- 字幕：比例估算
- 封面：Seedream 双参考图
- 截图：关闭（用资讯卡片）
- API 密钥：不需要
